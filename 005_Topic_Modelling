from __future__ import print_function
data_file = 'bersihkanindonesia.csv' # Make sure this file is in the same folder as the ipynb
n_topics, Top_Topics, Top_Words = 30, 7, 7 # Bergantung Kebutuhan analisa ... silahkan rubah
max_df, min_df = 0.75, 10 # For the VSM, silahkan sesuaikan sendiri
import numpy as np, pyLDAvis, pyLDAvis.sklearn; pyLDAvis.enable_notebook()
from tqdm import tqdm
from sklearn.feature_extraction.text import CountVectorizer, TfidfVectorizer
from sklearn.decomposition import LatentDirichletAllocation
from matplotlib import pyplot as plt
import MyLib as TS
